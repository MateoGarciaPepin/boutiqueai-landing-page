---
// Shared page-width wrapper. 1180px max, per PLAN.md "Layout".
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<div class:list={["mx-auto w-full max-w-[1180px] px-6 md:px-12", className]}>
  <slot />
</div>
